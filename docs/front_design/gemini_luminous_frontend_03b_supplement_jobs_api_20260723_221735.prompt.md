你是「栖光 luminous」前端体验与视觉重构的主设计师。请为 Batch 3B 补充 `/api/jobs` 的精确映射。不要重复全文，只输出补充节。

# Batch 3B Supplement：/api/jobs 后台任务审计映射

请覆盖：
1. GET /api/jobs 在 DebugTracePanel 中的位置与默认隐藏策略。
2. jobs 状态如何显示：running、success、failed、queued、stale/unknown。
3. 与 /api/trace、/api/ledger 的关系。
4. 普通用户不显示、开发/审计入口显示的规则。
5. 错误和弱网状态。
6. 自检清单。

要求具体、可实施、可验收；不要写完整代码补丁。
