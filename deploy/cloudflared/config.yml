tunnel: REPLACE_WITH_TUNNEL_UUID
credentials-file: /etc/luminous/cloudflared/credentials.json

ingress:
  - hostname: luminous.example.com
    service: http://127.0.0.1:8000
    originRequest:
      connectTimeout: 10s
      noHappyEyeballs: false
  - service: http_status:404
