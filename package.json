{
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:frontend",
    "test:frontend": "node --test tests/frontend/*.test.mjs",
    "test:real": "node tests/frontend/i1-real-mode-browser-acceptance.mjs",
    "test:browser": "node scripts/run-browser-acceptance.mjs",
    "build:android:web": "node scripts/build-android-web.mjs",
    "android:add": "npm run build:android:web && cap add android",
    "android:sync": "npm run build:android:web && cap sync android",
    "android:open": "cap open android",
    "android:build:debug": "node scripts/build-android-debug.mjs",
    "android:build:release": "node scripts/build-android-release.mjs"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "@playwright/test": "^1.62.0",
    "esbuild": "^0.25.12"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.0",
    "@capacitor/local-notifications": "^8.2.1"
  }
}
